<div class='field-unit__label'>
  <%= f.label field.attribute %>
</div>
<div class='field-unit__field'>
  <%= f.select(
        field.attribute,
        options_for_select(
          field.selectable_options.map { |k, v| [k.humanize, k] },
          field.data.presence
        )
      ) %>

</div>
