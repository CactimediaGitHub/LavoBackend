en:
  activemodel:
    errors:
      models:
        api/user/reset_password:
          attributes:
            user:
              user_presence_error: "can't find a user with this email"
            reset_digest:
              user_presence_error: "can't find a user by password reset token"
              expired: "password reset token expired"

